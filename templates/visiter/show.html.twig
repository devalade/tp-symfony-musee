{% extends 'base.html.twig' %}

{% block title %}Visiter{% endblock %}

{% block body %}
    <h1>Visiter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ visiter.id }}</td>
            </tr>
            <tr>
                <th>NumMus</th>
                <td>{{ visiter.numMus }}</td>
            </tr>
            <tr>
                <th>Jour</th>
                <td>{{ visiter.jour }}</td>
            </tr>
            <tr>
                <th>Nbvisiteurs</th>
                <td>{{ visiter.nbvisiteurs }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('visiter_index') }}">back to list</a>

    <a href="{{ path('visiter_edit', {'id': visiter.id}) }}">edit</a>

    {{ include('visiter/_delete_form.html.twig') }}
{% endblock %}
