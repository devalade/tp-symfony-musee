<form method="post" action="{{ path('visiter_delete', {'id': visiter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ visiter.id) }}">
    <button class="btn">Delete</button>
</form>
