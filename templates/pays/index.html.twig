{% extends 'base.html.twig' %}

{% block title%}Pays index{% endblock %}

{% block body %}
{#        <div class="min-h-screen flex items-center justify-center">#}

{#        <h1>Pays index</h1>#}

{#        <table class="">#}
{#            <thead>#}
{#                <tr>#}
{#                    <th>Id</th>#}
{#                    <th>CodePays</th>#}
{#                    <th>Nbhabitant</th>#}
{#                    <th>actions</th>#}
{#                </tr>#}
{#            </thead>#}
{#            <tbody>#}
{#            {% for pay in pays %}#}
{#                <tr>#}
{#                    <td>{{ pay.id }}</td>#}
{#                    <td>{{ pay.codePays }}</td>#}
{#                    <td>{{ pay.nbhabitant }}</td>#}
{#                    <td>#}
{#                        <a href="{{ path('pays_show', {'id': pay.id}) }}">show</a>#}
{#                        <a href="{{ path('pays_edit', {'id': pay.id}) }}">edit</a>#}
{#                    </td>#}
{#                </tr>#}
{#            {% else %}#}
{#                <tr>#}
{#                    <td colspan="4">no records found</td>#}
{#                </tr>#}
{#            {% endfor %}#}
{#            </tbody>#}
{#        </table>#}
{#    </div>#}

<div class="mt-12 w-3/4 mx-auto">
    <a class="bg-blue-700 text-blue-200 px-4 py-3 rounded" href="{{ path('pays_new') }}">Create new</a>
    <div class="flex flex-col mt-12">
        <div class="-my-2 overflow-x-auto sm:-mx-6 lg:-mx-8">
            <div class="py-2 align-middle inline-block min-w-full sm:px-6 lg:px-8">
                <div class="shadow overflow-hidden border-b border-gray-200 sm:rounded-lg">
                    <table class="min-w-full divide-y divide-gray-200">
                        <thead class="bg-gray-50">
                        <tr>
                            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                #
                            </th>
                            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                CodePays
                            </th>
                            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                Nombre Habitant
                            </th>
                            <th scope="col" class="relative px-6 py-3">
                            </th>
                        </tr>
                        </thead>
                        <tbody class="bg-white divide-y divide-gray-200">
                        {% for pay in pays %}
                            <tr>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <div class="flex items-center">
                                        <div class="ml-4 text-sm font-medium text-gray-900">
                                            {{ pay.id }}
                                        </div>
                                    </div>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                                    {{ pay.codePays }}
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    {{ pay.nbhabitant }}
                                </td>
                                <td class="flex px-6 py-4 space-x-2 whitespace-nowrap text-right text-sm font-medium">
                                    <a href="{{ path('pays_edit', {'id': pay.id}) }}" class="text-indigo-600 hover:text-indigo-900">Edit</a>
    {#                                <a href="{{ path('pays_delete', {'id': pay.id}) }}" class="text-red-600 hover:text-red-900">Delete</a>#}
                                    {{ include('pays/_delete_form.html.twig') }}
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td class="w-full text-3xl py-6 text-gray-600 text-center align-middle" colspan="4">no records found</td>
                            </tr>
                        {% endfor %}
                        <!-- More people... -->
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>


{% endblock %}

