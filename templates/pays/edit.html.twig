{% extends 'base.html.twig' %}

{% block title %}Edit Pays{% endblock %}

{% block body %}
<div class="w-full bg-gray-50 min-h-screen items-center justify-center">
    <div class="w-1/3 mx-auto p-3">
    <h1 class="my-4 text-gray-700 font-medium text-3xl uppercase">Edit Pays</h1>

    {{ include('pays/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="flex align-middle justify-between mt-6">
        <a href="{{ path('pays_index') }}" class="text-indigo-600">back to list</a>

        {{ include('pays/_delete_form.html.twig') }}
    </div>

    </div>
</div>
{% endblock %}
