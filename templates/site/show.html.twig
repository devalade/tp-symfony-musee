{% extends 'base.html.twig' %}

{% block title %}Site{% endblock %}

{% block body %}
    <h1>Site</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ site.id }}</td>
            </tr>
            <tr>
                <th>NomSite</th>
                <td>{{ site.nomSite }}</td>
            </tr>
            <tr>
                <th>Anneedecouv</th>
                <td>{{ site.anneedecouv ? site.anneedecouv|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>CodePays</th>
                <td>{{ site.codePays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('site_index') }}">back to list</a>

    <a href="{{ path('site_edit', {'id': site.id}) }}">edit</a>

    {{ include('site/_delete_form.html.twig') }}
{% endblock %}
